
name: Get latest data

on:
  push:
  workflow_dispatch:
  schedule:
    - cron:  '00 13 * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Check out this repo
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - uses: actions/cache@v2
      name: Configure pip caching
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Install Python dependencies
      run: |-
        pip install -r requirements.txt
    - name: Fetch latest data
      run: |-
        # Take all the existing state files and make one big file to compare to
        #csvstack *.csv > bmf-old.csv
        cp eo_fl.csv eo_fl-old.csv
        # Get all of the state files
        wget -i url_list.txt --quiet
        # Take all of the new state files and make another big file
        #csvstack *.csv > bmf.csv
        # Diff the changes and generate commit message using csv-diff
        csv-diff eo_fl-old.csv eo_fl.csv --key=EIN --singular=nonprofit --plural=nonprofits > message.txt
    - name: Commit and push if it changed
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add eo_*.csv
        timestamp=$(date -u)
        git commit -F message.txt || exit 0
        git push
